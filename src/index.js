/**
 * Placeholder entry point.
 *
 * Exists so `npm run build` and `npm run lint:js` have something to chew on
 * before any real code is written. wp-scripts builds src/index.js by default —
 * add a webpack.config.js if you want differently-named entries.
 */

// eslint-disable-next-line no-console
console.log( 'Pika POS: no terminal code yet.' );
